{
  "mcpServers": {
    "mcp-status": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/server.js"],
      "env": { "STATUS_CACHE_DIR": "${CLAUDE_PLUGIN_ROOT}/.cache" }
    }
  }
}
