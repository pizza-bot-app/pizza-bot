# Plugin examples

- [`mcp-status`](mcp-status) is a complete plugin with a small stdio MCP server
  and a skill that uses its tool. It has no external dependencies or build
  step.

Only plugins under the repository's top-level `plugins/` directory ship with
Pizza Bot. These examples are starting points for custom plugin bundles.
